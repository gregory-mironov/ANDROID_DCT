<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.login.LoginActivity">

    <include layout="@layout/activity_login_app_bar" />

    <EditText
        android:id="@+id/a_login_login_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="15dp"
        android:layout_marginTop="10dp"
        android:autofillHints="login"
        android:ems="10"
        android:hint="@string/label_login"
        android:inputType="textPersonName" />

    <EditText
        android:id="@+id/a_login_password_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="15dp"
        android:layout_marginTop="10dp"
        android:autofillHints="password"
        android:ems="10"
        android:hint="@string/label_password"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/a_login_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="15dp"
        android:layout_marginTop="10dp"
        android:background="@color/login_button_background"
        android:text="@string/label_log_in"
        android:textColor="@color/login_button_foreground" />

</LinearLayout>